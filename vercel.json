{
  "version": 2,
  "functions": {
    "server/api/**.js": {
      "runtime": "nodejs20.x"
    }
  },
  "builds": [{ "src": "server/api/**.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/", "dest": "server/api" },
    { "src": "/api(?:/.*)?", "dest": "server/api" }
  ]
}
